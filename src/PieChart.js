import React from "react";
import { axisTop } from 'd3-axis';

const PieChart = (props) => {
	// Принимает объект вида { 0: 12, 1: 34 } 
	console.log(props);
	return (
		<div className="pieChart"></div>
	)
}
export default PieChart;
